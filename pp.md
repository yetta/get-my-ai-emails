
# 2026-01-07

请帮我检查 Gmail 中来自 'The Rundown AI' 的未读邮件。如果找到了，请执行以下操作：

每日早上8点读取邮件正文内容。检查 Gmail 中来自 'The Rundown AI' 的未读邮件，只检查当前日期前两天的内容即可。

将其翻译为中文，并按照 '标题、摘要、核心要点、原文链接' 的 Markdown 格式进行排版。

翻译以及整理过程使用 minimax 模型或者智普模型 因为我已经购买了 api token。

使用邮件的标题作为文件名，标题体现出日期。将其保存到我的 Obsidian 笔记的 /News/AI-Updates/ 目录下。

完成后告诉我处理了多少封。

# 2026-01-16

生成的笔记内容需要配图
我的邮件原文是有配图的，请帮我提取图片，并保存到笔记内容中。

# 2026-01-17

优化下 get-my-ai-emails 中 index.js 的处理逻辑
在 get-my-ai-emails 中，有时候生成的笔记内容不太符合我的要求，需要调整生成逻辑，但是每次调试都需要把整个功能流程走一遍，我不想要每次都调用gmial接口获取邮件，解析邮件内容，翻译等等，为了节省gmial接口调用次数，把程序中间过程的产物存入本地，比如把 gmailService.getEmailContent(message.id) 的结果存入本地，目录为 emials, 文件名使用 message.id + ".html",
把 aiTranslator.translate(parsed.markdown, links) 的结果存入本地，目录为 translated, 文件名使用 message.id + ".md"，在 index.js 的处理流程中，先判断本地是否有对应的文件，如果有，就直接使用本地文件，如果没有，就调用接口获取。


# 2026-01-20

## 减少 AI 调用的输入 token

1. 邮件内容读取范围：内容从“Good morning, yetta.”开始，到“<b>COMMUNITY</b>”结束，不包括“<b>COMMUNITY</b>”
2. 生成的md文档不要出现“邮件”关键词
3. 其他减少输入token但不影响邮件原文理解的信息也可以节省掉

## 输入内容再精简

我发现生成的md 包含广告，广告内容大概是一下内容，全部删掉，不要出现广告的内容
- [联系信息]() -  获取更多关于 AI 资讯的信息。
- [免费注册]() -  注册以获取更多 AI 资讯。
- [AI 大学]() -  加入 AI 大学，学习更多 AI 知识。

## 不需要运行测试，只改动代码


## 一级新闻生成冗余

我发现每次生成的md文档，第一条一级新闻会在md文档的“核心内容”中生成多条记录，而且会引用其他一级新闻的图片，请检查原因，并修复每个一级新闻在“核心内容”中，只出现一次。

## 把md文档顶部的发布日期去掉，直接展示内容即可

## 不需要运行测试，只改动代码

# 2026-01-20

## 流程图

现在我需要把这个项目分享给我的团队伙伴，让他们了解我是如何实现 AI 资讯自动获取、翻译、整理、保存到 Obsidian 的。给我输出一个尽量详细的流程图，流程图要采用什么格式我还没有想好，给我推荐一个合适的流程图格式。

# 2026-01-22
 
今天更新了 LLM 调用超时的重试机制，给我生成最新的流程图，用于向我的团队伙伴展示分享。流程图格式采用 excalidraw 格式。







